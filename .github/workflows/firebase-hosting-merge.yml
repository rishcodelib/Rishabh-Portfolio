# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - master
jobs:
  #build Job
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Setup node
        uses: actions/setup-node@v2
        with:
          node-version: "10.x"

      - name: Install Dependencies
        run:
          npm install

          # Runs a set of commands to build
      - name: Build
        run:
          npm run build --prod

          # sotre the artifact
      - name: Archive Production Artifact
        uses: actions/upload-artifact@master
        with:
          name: public
          path: ./public
        #Deploy job
  deploy:
    name: Deploy to firebase
    runs-on: ubuntu-latest
    # only run if build job complte successfully
    needs: [build]
    steps:
    - uses: actions/checkout@master
    - uses: actions/setup-node@master
      with:
        node-version: '10.x'
        # Install the dependencies need to build the app
    - name: Install Dependencies
      run: npm install
   # build the app
    - name: Build the app artifact for prod
      run: npm run build --prod
      
    - uses: FirebaseExtended/action-hosting-deploy@v0
      with:
        repoToken: "${{ secrets.GITHUB_TOKEN }}"
        firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_RISHABHAGGARWAL_BAE83 }}"
        channelId: live
        projectId: rishabhaggarwal-bae83
